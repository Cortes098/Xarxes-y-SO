

***	1 pipe para cada hijo	***

//Codigo
void main() 
{
	int pipe1[2];
	int pipe2[2];

	int ph1 pipe(pipe1);
	int ph1 pipe(pipe2);

	if (fork() == 0) 
	{
	
	}
	else if(fork()==0)
	{
	
	}
	else 
	{
		
	}
}

2 hijos envian al padre

1 pipe, lee pero no sabe quien a no seeeeeeeeer SIGUSR1, SIGUSR2;
para comunicar los hijos la pipe en el padre;

